#version 410

uniform vec3 sun_dir;
uniform vec3 color;
uniform vec3 ambient;

struct GSOut {
  vec3 vertex;
  vec3 normal;
  vec4 bonew;  
};

in GSOut gsout;

out vec4 output;

void main(void)
{
  float d = max(0, dot(sun_dir, gsout.normal));
  output = vec4(color * d + ambient, 1);
}
